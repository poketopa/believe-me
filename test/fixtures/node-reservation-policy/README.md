# Node reservation policy fixture

This dependency-free fixture proves that the packaged CLI is not tied to
Spring. Its manifest selects the bounded `command-verifier` with exact argv
`node --test`. The deterministic candidate restores the inclusive remaining-seat
boundary, then the installed tarball runs, receipts, and applies it through the
same frozen-manifest verifier.
